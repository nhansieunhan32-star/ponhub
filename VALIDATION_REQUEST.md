# Linux Validation Request V1

Build exact upstream commit `7580820d52a5b17d8124e02903e655c0d0e2a628`, apply the engine-native Root Guard patch, run UCI smoke tests, built-in benchmark, and ASan/UBSan validation, then publish the Linux binaries and logs as an artifact.
